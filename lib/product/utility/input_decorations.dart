import 'package:flutter/material.dart';

class ProjectInputs extends InputDecoration {
  const ProjectInputs(String title)
      : super(border: const OutlineInputBorder(), labelText: title, focusColor: Colors.red);
}
